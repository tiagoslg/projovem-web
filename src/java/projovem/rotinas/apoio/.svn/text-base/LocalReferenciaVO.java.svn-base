/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package projovem.rotinas.apoio;

import java.io.Serializable;
import projovem.rotinas.CE.R003.CeRotina003;
import projovem.rotinas.CE.R004.CeRotina004;
import projovem.rotinas.R001.Rotina001;
import projovem.rotinas.R002.Rotina002;
import projovem.rotinas.R003.Rotina003;
import projovem.rotinas.R006.Rotina006ArcosOcupacionais;
import projovem.rotinas.R006.Rotina006ArcosOcupacionaisV3;
import projovem.rotinas.R006.Rotina006CoordenacaoLocal;

/**
 *
 * @author admprojovem
 */
public class LocalReferenciaVO implements Serializable {

    private String cepLocal;
    private String enderecoLocal;
    private String nomeLocal;
    private String bairroLocal;
    private String numeroLocal;
    private String complementoLocal;
        private String cidadeLocal;
    private String ufLocal;
    private String telefoneLocal;

    public String getBairroLocal() {
        return bairroLocal;
    }

    public Rotina002 obterDadosLocal(Rotina002 rotina) {
        rotina.setBairroLocal(getBairroLocal());
        rotina.setCepLocal(getCepLocal());
        rotina.setCidadeLocal(getCidadeLocal());
        //rotina.setComplementoLocal(getComplementoLocal());
        rotina.setEnderecoLocal(getEnderecoLocal());
       // rotina.setNomeLocal(getNomeLocal());
        //rotina.setNumeroLocal(getNumeroLocal());
        //rotina.setTelefoneLocal(getTelefoneLocal());
        rotina.setUfLocal(getUfLocal());
        return rotina;
    }

    public Rotina001 obterDadosLocal(Rotina001 rotina) {
        rotina.setBairroLocal(getBairroLocal());
        rotina.setCepLocal(getCepLocal());
        rotina.setCidadeLocal(getCidadeLocal());
        rotina.setComplementoLocal(getComplementoLocal());
        rotina.setEnderecoLocal(getEnderecoLocal());
        rotina.setNomeLocal(getNomeLocal());
        rotina.setNumeroLocal(getNumeroLocal());
        rotina.setTelefoneLocal(getTelefoneLocal());
        rotina.setUfLocal(getUfLocal());
        return rotina;
    }

    public Rotina003 obterDadosLocal(Rotina003 rotina) {
        rotina.setBairroLocal(getBairroLocal());
        rotina.setCepLocal(getCepLocal());
        rotina.setCidadeLocal(getCidadeLocal());
        rotina.setComplementoLocal(getComplementoLocal());
        rotina.setEnderecoLocal(getEnderecoLocal());
        rotina.setNomeLocal(getNomeLocal());
        rotina.setNumeroLocal(getNumeroLocal());
        rotina.setTelefoneLocal(getTelefoneLocal());
        rotina.setUfLocal(getUfLocal());
        return rotina;
    }
    public CeRotina004 obterDadosLocal(CeRotina004 rotina) {
        rotina.setBairroLocal(getBairroLocal());
        rotina.setCepLocal(getCepLocal());
        rotina.setCidadeLocal(getCidadeLocal());
        rotina.setComplementoLocal(getComplementoLocal());
        rotina.setEnderecoLocal(getEnderecoLocal());
        rotina.setNomeLocal(getNomeLocal());
        rotina.setNumeroLocal(getNumeroLocal());
        rotina.setTelefoneLocal(getTelefoneLocal());
        rotina.setUfLocal(getUfLocal());
        return rotina;
    }

    public Rotina006CoordenacaoLocal obterDadosLocal(Rotina006CoordenacaoLocal rotina) {
        rotina.setBairroLocal(getBairroLocal());
        rotina.setCepLocal(getCepLocal());
        rotina.setCidadeLocal(getCidadeLocal());
        rotina.setComplementoLocal(getComplementoLocal());
        rotina.setEnderecoLocal(getEnderecoLocal());
        rotina.setNomeLocal(getNomeLocal());
        rotina.setNumeroLocal(getNumeroLocal());
        rotina.setTelefoneLocal(getTelefoneLocal());
        rotina.setUfLocal(getUfLocal());
        return rotina;
    }

    public CeRotina003 obterDadosLocal(CeRotina003 rotina) {
        rotina.setBairroLocal(getBairroLocal());
        rotina.setCepLocal(getCepLocal());
        rotina.setCidadeLocal(getCidadeLocal());
        rotina.setComplementoLocal(getComplementoLocal());
        rotina.setEnderecoLocal(getEnderecoLocal());
        rotina.setNomeLocal(getNomeLocal());
        rotina.setNumeroLocal(getNumeroLocal());
        rotina.setTelefoneLocal(getTelefoneLocal());
        rotina.setUfLocal(getUfLocal());
        return rotina;
    }

    public Rotina006ArcosOcupacionais obterDadosLocal(Rotina006ArcosOcupacionais rotina) {
        rotina.setBairroLocal(getBairroLocal());
        rotina.setCepLocal(getCepLocal());
        rotina.setCidadeLocal(getCidadeLocal());
        rotina.setComplementoLocal(getComplementoLocal());
        rotina.setEnderecoLocal(getEnderecoLocal());
        rotina.setNomeLocal(getNomeLocal());
        rotina.setNumeroLocal(getNumeroLocal());
        rotina.setTelefoneLocal(getTelefoneLocal());
        rotina.setUfLocal(getUfLocal());
        return rotina;
    }

    public Rotina006ArcosOcupacionaisV3 obterDadosLocal(Rotina006ArcosOcupacionaisV3 rotina) {
        rotina.setBairroLocal(getBairroLocal());
        rotina.setCepLocal(getCepLocal());
        rotina.setCidadeLocal(getCidadeLocal());
        rotina.setComplementoLocal(getComplementoLocal());
        rotina.setEnderecoLocal(getEnderecoLocal());
        rotina.setNomeLocal(getNomeLocal());
        rotina.setNumeroLocal(getNumeroLocal());
        rotina.setTelefoneLocal(getTelefoneLocal());
        rotina.setUfLocal(getUfLocal());
        return rotina;
    }

    public void setBairroLocal(String bairroLocal) {
        this.bairroLocal = bairroLocal;
    }

    public String getCepLocal() {
        return cepLocal;
    }

    public void setCepLocal(String cepLocal) {
        this.cepLocal = cepLocal;
    }

    public String getCidadeLocal() {
        return cidadeLocal;
    }

    public void setCidadeLocal(String cidadeLocal) {
        this.cidadeLocal = cidadeLocal;
    }

    public String getComplementoLocal() {
        return complementoLocal;
    }

    public void setComplementoLocal(String complementoLocal) {
        this.complementoLocal = complementoLocal;
    }

    public String getEnderecoLocal() {
        return enderecoLocal;
    }

    public void setEnderecoLocal(String enderecoLocal) {
        this.enderecoLocal = enderecoLocal;
    }

    public String getNomeLocal() {
        return nomeLocal;
    }

    public void setNomeLocal(String nomeLocal) {
        this.nomeLocal = nomeLocal;
    }

    public String getNumeroLocal() {
        return numeroLocal;
    }

    public void setNumeroLocal(String numeroLocal) {
        this.numeroLocal = numeroLocal;
    }

    public String getUfLocal() {
        return ufLocal;
    }

    public void setUfLocal(String ufLocal) {
        this.ufLocal = ufLocal;
    }

    /**
     * @return the telefoneLocal
     */
    public String getTelefoneLocal() {
        return telefoneLocal;
    }

    /**
     * @param telefoneLocal the telefoneLocal to set
     */
    public void setTelefoneLocal(String telefoneLocal) {
        this.telefoneLocal = telefoneLocal;
    }
}
